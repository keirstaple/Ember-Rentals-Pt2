

<li> {{announcements.city}} {{announcements.date}}: {{announcements.description}}
  {{#if announcementShowing }}
  <p{{action 'announcementShow'}}></p>
  {{else}}
  <button {{action 'announcementHide'}}>Hide Announcement</button>
  {{/if}}
  <button {{action 'delete' announcements}}>Delete</button>
</li>
